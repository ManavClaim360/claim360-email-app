# Frontend environment variables
# Copy this to frontend/.env

# Backend API base URL
# Local dev: leave blank (Vite proxy handles /api → localhost:8000)
# Production: set to your backend Vercel URL
VITE_API_URL=
