import asyncio, os, sys; sys.path.append(os.getcwd()); from core.database import AsyncSessionLocal; from models.user import User; from core.auth import get_password_hash; from sqlalchemy import select; async def reset(): async with AsyncSessionLocal() as s: u = (await s.execute(select(User).where(User.email == 'manav@claim360.in'))).scalar_one_or_none(); if u: u.hashed_password = get_password_hash('Manav360#'); await s.commit(); print('? Admin reset: Manav360#'); else: print('? Admin not found'); asyncio.run(reset()) 
